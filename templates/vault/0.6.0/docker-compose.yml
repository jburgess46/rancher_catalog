vault:
  net: 'container:vault-config'
  restart: always
  tty: true
  links:
    - consul
  volumes_from:
    - vault-data-volume
  command:
    - vault
    - server
    - --config=/vault/conf/vault.hcl
  cap_add:
    - IPC_LOCK
  image: jburgess46/vault-9
  stdin_open: true
  labels:
    io.rancher.sidekicks: vault-data-volume, vault-config
vault-config:
  image: jburgess46/vault-config-9
  labels:
    io.rancher.sidekicks: vault-data-volume
  volumes_from:
    - vault-data-volume
  ports:
    - "8200:8200"
vault-data-volume:
  image: busybox
  net: none
  command: /bin/true
  labels:
    io.rancher.container.start_once: true
  volumes:
    - /vault/conf
